@author yishi
@desc 职责链模式使用不确定的链元素作为请求处理者。通过定义单一链元素，使之持有下个链元素的引用，一直传递下去。
@more 职责链模式的任务和链的组成由外部定义，如果使用不当可能会造成循环调用导致死循环。
@more 设想不用引用而使用有序集合如list装载职责处理者，遍历集合处理请求，请求成功则break，这样职责链模式就和订阅-发布模式有些相像。
@more 可能会存在多个职责处理者的处理条件相同，前者拦截请求，后者收不到请求的情况。